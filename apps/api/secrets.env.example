# Example secrets file for the Dialogue Branch Web Service.
#
# Copy this file to secrets.env, fill in your values, and reference it in
# your docker-compose.yml:
#
#   env_file:
#     - secrets.env
#
# Never commit secrets.env to version control.

# -----------------------------------------------------------------------
# Native authentication secrets (required when AUTH_SERVICE=native)
# Use a long, random Base64 string for each — e.g.:
#   openssl rand -base64 64
# -----------------------------------------------------------------------
AUTH_NATIVE_JWT_ACCESS_TOKEN_SECRET=
AUTH_NATIVE_JWT_REFRESH_TOKEN_SECRET=

# -----------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------
MARIADB_PASSWORD=

# -----------------------------------------------------------------------
# Keycloak (required when AUTH_SERVICE=keycloak)
# -----------------------------------------------------------------------
AUTH_KEYCLOAK_CLIENT_SECRET=

# -----------------------------------------------------------------------
# Keycloak admin (required when running the full docker-compose stack)
# -----------------------------------------------------------------------
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=

# -----------------------------------------------------------------------
# External Variable Service (required when EXTERNAL_VARIABLE_SERVICE_ENABLED=true)
# -----------------------------------------------------------------------
EXTERNAL_VARIABLE_SERVICE_URL=
EXTERNAL_VARIABLE_SERVICE_API_KEY=

# -----------------------------------------------------------------------
# Azure Data Lake (required when AZURE_DATA_LAKE_ENABLED=true)
# -----------------------------------------------------------------------
AZURE_DATA_LAKE_ACCOUNT_KEY=
AZURE_DATA_LAKE_SAS_TOKEN=
